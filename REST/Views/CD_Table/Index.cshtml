@{
    ViewData["Title"] = "Store Back";
    ViewData["System"] = "โต๊ะ";
    Layout = "_StoreBack";
}

@section Styles {
    <link rel="stylesheet" href="~/Plugins/DataTables/DataTables-1.10.21/css/dataTables.bootstrap4.min.css" />
}

<div class="card rounded-0">
    <div class="card-body">
        <div class="row">
            <div class="col-xl mx-auto">
                <!-- Header -->
                <section>
                    <div class="d-flex mb-2">
                        <div class="mr-2">
                            <a href="@Url.Action("FrmTable", "CD_Table", new { mode = "Add"})" class="text-success col-form-label-sm"><i class="fas fa-folder-plus"></i>&nbsp;เพิ่มโต๊ะ</a>
                        </div>
                    </div>
                    <hr />
                </section>
                <!-- Table -->
                <section class="Frm-Table">
                    <table id="DT_Table" class="table table-md table-hover table-bordered">
                        <thead class="thead-primary-light">
                            <tr>
                                <th class="text-center" style="width:20%;"> รหัส </th>
                                <th class="text-center"> ชื่อ </th>
                                <th class="text-center" style="width:10%;"> ที่นั่ง </th>
                                <th class="text-center" style="width:20%;"> โซนที่นั่ง </th>
                                <th class="text-center" style="width:5%;"> สาขา </th>
                                <th class="text-center" style="width:5%;"> <i class="fas fa-info"></i> </th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var row in ViewBag.DT_Table)
                            {
                                <tr>
                                    <td>@row.TableId</td>
                                    <td>@row.TableName</td>
                                    <td class="text-center">@row.Personal</td>
                                    <td>@row.ZoneName</td>
                                    <td>@row.BchName</td>
                                    <td class="text-center"><a href="@Url.Action("FrmTable", "CD_Table", new { mode = "Edit", id = row.TableId})" class="text-info"><i class="fas fa-edit"></i></a></td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </section>
            </div>
        </div>        
    </div>
</div>

@section Scripts {
    <script src="~/Plugins/DataTables/datatables.min.js"></script>
    <script src="~/Plugins/DataTables/DataTables-1.10.21/js/dataTables.bootstrap4.min.js"></script>
    <script>
        $(document).ready(function () {
            $('#DT_Table').DataTable({
                "info": false,
                "ordering": false
            });

            $(".item_click_Table").addClass("active");

            $('[scroll]').slimScroll({
                height: $(window).height() - 95,
                railBorderRadius: 0,
                railVisible: true
            });
        });
    </script>
}